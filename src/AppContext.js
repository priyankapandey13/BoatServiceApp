import { createContext } from "react";

const UserContext = createContext({
  // IsUserLogin : false,
  // LoginUserInfo: {},
});
// if user is login
// 1. check user is login by status
// 2. get the user details

// export { UserContext };
export default UserContext;
